name: First CI/CD Pipeline
on:
  workflow_dispatch:
  push:
    branches: 
      - mainapp
      - develop
jobs:
  clone:
     runs-on: server1
     steps:
      - name: Git clone
        run: |
          cd /home/ubuntu
          git pull origin mainapp
